# Core
chromadb>=0.6.0
sentence-transformers>=3.0.0
python-dotenv>=1.0.0     # loads .env into environment variables

# Document parsing
pymupdf>=1.24.0          # PDFs  (imported as `fitz`)
python-docx>=1.1.0       # Word documents

# LLM backend
anthropic>=0.28.0        # Claude (Anthropic)
