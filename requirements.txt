# Core
chromadb>=0.6.0
sentence-transformers>=3.0.0

# Document parsing
pymupdf>=1.24.0          # PDFs  (imported as `fitz`)
python-docx>=1.1.0       # Word documents

# LLM backends (install both; only the one you configure will be used)
ollama>=0.3.0            # Local LLM via Ollama (https://ollama.com)
openai>=1.0.0            # OpenAI cloud API
